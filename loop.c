#include<stdio.h>
int main()
{
    int n, m, sum;
    printf("enter the digits for sum=");
    scanf("%d %d",&m, &n);
     
     while(m<=n)
     {
        printf("sum= %d\n", sum=m+n);
        m=m+1;
     }
     
}